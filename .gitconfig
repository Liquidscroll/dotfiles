[core]
	autocrlf = input
[user]
	email = jojaty@gmail.com
	name = Liquidscroll
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
