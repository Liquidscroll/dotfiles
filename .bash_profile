# shellcheck shell=bash
#
# ~/.bash_profile

# shellcheck source="/home/liquidscroll/.profile"
[ -f "$HOME/.profile" ] && . "$HOME/.profile"
# shellcheck source="/home/liquidscroll/.bashrc"
[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"
